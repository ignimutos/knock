---
paths:
  - 'README.md'
  - 'config.example.yml'
  - 'src/main.ts'
  - 'src/bootstrap/**'
  - 'src/config/**'
  - 'src/contracts/**'
  - 'src/adapters/**'
  - 'src/workflow/**'
  - 'src/persistence/**'
  - 'src/web/**'
  - 'web/**'
---

# docs-sync

- 行为、配置 shape、命名、CLI 输出或错误文案变化时，MUST 同步检查 `README.md` 与 `config.example.yml`。
- MUST NOT 记录代码未实现的行为。
